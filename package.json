{
  "name": "clawos",
  "version": "0.3.1",
  "type": "module",
  "scripts": {
    "tailwind:build": "tailwindcss -i src/styles/input.css -o dist/output.css --minify",
    "tailwind:watch": "tailwindcss -i src/styles/input.css -o dist/output.css --watch=always",
    "dev": "bun run tailwind:build && bun --watch src/server.ts",
    "electrobun:dev": "bun run scripts/electrobun.ts dev",
    "electrobun:dev:watch": "bun run scripts/electrobun.ts dev --watch",
    "electrobun:build": "bun run scripts/electrobun.ts build",
    "desktop:dev": "bun run scripts/desktop-dev.ts",
    "desktop:dev:raw": "bun run tailwind:build && CLAWOS_DESKTOP_DEV=1 bun run scripts/electrobun.ts dev --watch",
    "desktop:build": "bun run tailwind:build && bun run scripts/electrobun.ts build",
    "desktop:build:canary": "bun run tailwind:build && bun run scripts/electrobun.ts build --env=canary",
    "desktop:build:stable": "bun run tailwind:build && bun run scripts/electrobun.ts build --env=stable",
    "build": "bun run build:exe",
    "publish": "bun run publish:clawos",
    "publish:json": "bun run scripts/publish-clawos.ts --skip-installer",
    "start": "bun run src/server.ts",
    "test": "bun test",
    "publish:clawos": "bun run scripts/publish-clawos.ts",
    "build:exe": "bun run scripts/build-clawos.ts"
  },
  "dependencies": {
    "json5": "^2.2.3",
    "viem": "^2.46.3"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.16",
    "daisyui": "^5.5.18",
    "electrobun": "^1.13.1",
    "png-to-ico": "^3.0.1",
    "tailwindcss": "^4.1.16"
  }
}
