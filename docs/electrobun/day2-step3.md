# Electrobun 迁移 Day 2 - Step 3

## 本步完成
- 桌面壳增加单实例守护（本地控制端口）
- 次实例启动时不再创建新窗口，而是通知首实例聚焦窗口后退出

## 机制说明
- 控制端口默认值：`8151`
- 可通过环境变量覆盖：`CLAWOS_DESKTOP_CONTROL_PORT`
- 控制协议：次实例向 `127.0.0.1:<controlPort>` 发送 `focus`

## 结果
- 避免重复启动多个 ClawOS 桌面窗口
- 用户重复点击启动图标时，体验为“唤醒已有窗口”
