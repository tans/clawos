#!/usr/bin/env bash
set -euo pipefail

if [ "${SKIP_TEST_ON_COMMIT:-0}" = "1" ]; then
  echo "[pre-commit] SKIP_TEST_ON_COMMIT=1, skip tests"
  exit 0
fi

echo "[pre-commit] running tests: bun test test"
bun test test
